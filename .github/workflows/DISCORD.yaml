      - name: Send GitHub Event Notification to Discord
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_MASTER_URL }}
        run: |
          set -x  # Enable debugging
          
          SHORT_MARKDOWN_CONTENT=$(echo "$MARKDOWN_CONTENT" | head -c 1000)

          JSON_PAYLOAD=$(jq -n \
            --arg timestamp "$TIMESTAMP" \
            --arg repo "$REPO" \
            --arg markdown_url "$MARKDOWN_URL" \
            --arg tag "$TAG_NAME" \
            --arg version "$VERSION_NAME" \
            --arg markdown_content "$SHORT_MARKDOWN_CONTENT" \
            '{
              "username": "GitHub Bot",
              "avatar_url": "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png",
              "embeds": [{
                "title": "🚀 Release \($version)",
                "description": "We are so excited to announce the latest release of our project! This update includes key improvements and enhancements to make our system even better.\n\n",
                "fields": [
                  { "name": "📅 Timestamp", "value": $timestamp, "inline": false },
                  { "name": "📂 Repository", "value": "[\($repo)](https://github.com/\($repo))", "inline": false },
                  { "name": "📜 Markdown Documentation", "value": "[Click here](\($markdown_url))\n\nMarkdown Preview:\n```\n\($markdown_content)\n```", "inline": false },
                  { "name": "🏷️ Tag", "value": "\($tag) - \($timestamp)", "inline": false },
                  { "name": "🚀 Version", "value": "Release \($version)", "inline": false }
                ]
              }]
            }')

          echo "JSON Payload: $JSON_PAYLOAD"  # Debug output

          curl -H "Content-Type: application/json" -X POST -d "$JSON_PAYLOAD" "$DISCORD_WEBHOOK_URL"
